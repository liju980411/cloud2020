<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.atguigu.springcloud.dao.OrderDao">

    <resultMap id="order" type="com.atguigu.springcloud.domain.Order">
        <id column="id" property="id" jdbcType="bigint"/>
        <result column="user_id" property="userId" jdbcType="bigint"/>
        <result column="product_id" property="productId" jdbcType="bigint"/>
        <result column="count" property="count" jdbcType="int"/>
        <result column="money" property="money" jdbcType="decimal"/>
        <result column="status" property="status" jdbcType="int"/>
    </resultMap>

    <insert id="create" parameterMap="order">
        insert into t_order(id,user_id,product_id,count,money,status) values (null,#{userId},#{productId},#{count},#{money},0)
    </insert>

    <update id="update">
        update t_order set count=#{count},status=1 where userId=#{userId} and status=#{status}
    </update>
</mapper>